---
/**
 * Standard inner page: a header block with eyebrow, title and intro,
 * followed by whatever the page puts in the default slot.
 */
import BaseLayout from './BaseLayout.astro';
import Breadcrumbs from '~/components/Breadcrumbs.astro';

interface Props {
  title: string;
  description?: string;
  eyebrow?: string;
  /** Visible page heading. Falls back to `title`. */
  heading?: string;
  intro?: string;
  crumbs?: { label: string; href?: string }[];
  image?: string;
  imageAlt?: string;
  noindex?: boolean;
  jsonLd?: string;
  /** Set false to render your own header block in the slot. */
  showHeader?: boolean;
}

const {
  title,
  description,
  eyebrow,
  heading,
  intro,
  crumbs,
  showHeader = true,
  ...seo
} = Astro.props;
---

<BaseLayout title={title} description={description} {...seo}>
  {
    showHeader && (
      <section class="wrap phero">
        <div class="phero-in rev">
          {crumbs && <Breadcrumbs items={crumbs} />}
          {eyebrow && <p class="eyebrow">{eyebrow}</p>}
          <h1 class="d1" style="font-size:clamp(38px,6.4vw,76px)">{heading ?? title}</h1>
          {intro && <p class="lead">{intro}</p>}
          <slot name="header-extra" />
        </div>
      </section>
    )
  }
  <slot />
</BaseLayout>
